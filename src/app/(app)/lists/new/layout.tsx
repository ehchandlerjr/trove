import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Create New List | Trove',
};

export default function NewListLayout({ children }: { children: React.ReactNode }) {
  return children;
}
